# TWITTERITY ENVIRONMENT CONFIGURATION
# Copy this file to .env and configure the settings for your environment

# Database Configuration
# Supported values: sqlite, mysql
DB_TYPE=sqlite

# SQLite Configuration (used when DB_TYPE=sqlite)
DB_SQLITE_PATH=database.sqlite

# MySQL Configuration (used when DB_TYPE=mysql)
DB_MYSQL_HOST=db
DB_MYSQL_PORT=3306
DB_MYSQL_DATABASE=twitterity
DB_MYSQL_USERNAME=root
DB_MYSQL_PASSWORD=
DB_MYSQL_CHARSET=utf8mb4

# Application Settings
APP_NAME=Twitterity
APP_ENV=development
APP_DEBUG=true

# Setup Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your settings
# 3. For MySQL: Create database first
# 4. Test config: php test-db.php

# Example Configurations:
# For SQLite (default): DB_TYPE=sqlite
# For MySQL: DB_TYPE=mysql + configure MySQL settings above
# For Production: APP_ENV=production, APP_DEBUG=false
